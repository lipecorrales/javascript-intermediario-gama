function soma(x, y){
    return x + y;
}

const multiplicacao = function (x, y) {
    return x * y;
};

const dividir = (x, y) => {
    return x / y;
};

const objeto = () => ({ nome: 'Philippe', sobrenome: 'Corrales'});